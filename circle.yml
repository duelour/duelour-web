machine:
  timezone:
    Australia/Melbourne
  node:
    version: 7.8.0
deployment:
  production:
    branch: master
    commands:
      - sudo ./internals/deploy.sh prod
  staging:
    branch: development
    commands:
      - sudo ./internals/deploy.sh dev
